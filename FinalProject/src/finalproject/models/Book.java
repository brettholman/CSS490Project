package finalproject.models;

public class Book extends InventoryItem{
	/* nothing really to change from inventory item. */ 
	// Since there will be only one type of item in the inventory, it is a little unnecessary to 
	// change have a derived class from inventory item. So I may end up taking this out. 
}
